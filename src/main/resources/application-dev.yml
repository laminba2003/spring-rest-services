spring:
    datasource:
        password: passer
        url: jdbc:mysql://localhost:3306/example?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
        username: root
        jpa:
            database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: http://localhost:8083/auth/realms/example
                    jwk-set-uri: http://localhost:8083/auth/realms/example/protocol/openid-connect/certs
